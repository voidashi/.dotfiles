# =====================================================================
#  AUTOSTART
# =====================================================================
# Inicialização automática de processos necessários, como notificações e status bars
# exec-once = $terminal &

exec-once = rfkill block bluetooth

exec-once = nm-applet &
# exec-once = waybar &
exec-once = waybar -c .config/waybar/fixed/config.jsonc -s .config/waybar/fixed/style.css &
exec-once = swaybg -m fill -i "$(bash $HOME/scripts/select_random_wallpaper.sh $HOME/Pictures/Wallpapers)"

exec-once = kill mako & swaync


# Prefer dark theme
env = QT_QPA_PLATFORMTHEME,qt6ct   # for Qt apps# Theme
