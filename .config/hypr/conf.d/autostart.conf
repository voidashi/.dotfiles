# =====================================================================
#  AUTOSTART
# =====================================================================
# Inicialização automática de processos necessários, como notificações e status bars
# exec-once = $terminal &
exec-once = nm-applet &
# exec-once = waybar &
exec-once = waybar -c .config/waybar/fixed/config.jsonc -s .config/waybar/fixed/style.css &
exec-once = swaybg -m fill -i "$(bash $HOME/scripts/wm-utils/select_random_wallpaper.sh $HOME/Pictures/Current_wallpapers)"
